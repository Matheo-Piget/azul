// Logique de scoring spécifique à Azul Classique
import { GameState } from '../../models/types';

export function calculateClassicScore(gameState: GameState): number {
  // TODO: Calculer le score selon les règles classiques
  return 0;
} 